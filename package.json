{
  "name": "tmdb-api",
  "version": "1.0.0",
  "description": "TMDB API",
  "main": "entry.js",
  "scripts": {
    "DO-NOT-USE": "echo \"🚫 Adding scripts here causes them to run on each developers local machine under the operating system and NodeJS version they have installed and is specific to each developers environment. A better approach is to run scripts in Docker and specify the details (including the OS and NodeJS version) in the docker-compose.yml\""
  },
  "author": "Austin Maddox",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "axios": "^0.19.2",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "debug": "^4.1.1",
    "dotenv": "^8.2.0",
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.0",
    "eslint-plugin-jest": "^23.8.2",
    "eslint-plugin-prettier": "^3.1.2",
    "jest": "^25.1.0",
    "jest-junit": "^10.0.0",
    "nodemon": "^2.0.2",
    "prettier": "^2.0.5"
  }
}
